# 📦 Встановлення Boxing Manager

## 🚀 Швидкий Старт

### Варіант 1: GitHub Pages (РЕКОМЕНДУЮ)

```bash
# 1. Створи новий репозиторій на GitHub
# 2. Клонуй його
git clone https://github.com/YOUR_USERNAME/boxing-manager.git
cd boxing-manager

# 3. Скопіюй всі файли з цієї папки

# 4. Commit та push
git add .
git commit -m "Initial commit - Boxing Manager v0.90"
git push origin main

# 5. Увімкни GitHub Pages
# Settings → Pages → Source: main branch → Save

# 6. Твоя гра буде доступна на:
# https://YOUR_USERNAME.github.io/boxing-manager/
```

### Варіант 2: Netlify

```bash
# 1. Зареєструйся на netlify.com
# 2. Drag & Drop цю папку на Netlify
# 3. Готово! Отримаєш URL типу: https://your-app.netlify.app
```

### Варіант 3: Vercel

```bash
# 1. Встанови Vercel CLI
npm install -g vercel

# 2. Задеплой
cd boxing-manager
vercel

# 3. Готово!
```

## 🤖 Налаштування Telegram Bot

### Крок 1: Створи бота

```
1. Відкрий @BotFather в Telegram
2. Надішли /newbot
3. Вибери ім'я: "Boxing Manager"
4. Вибери username: "YourBoxingManagerBot"
5. Збережи токен!
```

### Крок 2: Налаштуй Web App

```
1. Надішли @BotFather команду: /newapp
2. Вибери свого бота
3. Введи назву: "Boxing Manager"
4. Опис: "Керуй кар'єрою боксера!"
5. Фото: (завантаж іконку)
6. Web App URL: https://YOUR_URL/
7. Short name: boxing
```

### Крок 3: Тестування

```
1. Відкрий свого бота в Telegram
2. Натисни "Play 🎮" або відправ /start
3. Гра має відкритись!
```

## 🔧 Локальна Розробка

### З Python:
```bash
cd boxing-manager
python3 -m http.server 8000
# Відкрий: http://localhost:8000
```

### З Node.js:
```bash
npx http-server -p 8000
# Відкрий: http://localhost:8000
```

### З PHP:
```bash
php -S localhost:8000
```

## 📁 Структура Файлів

```
boxing-manager/
├── index.html          # Головний файл (38 KB)
├── css/
│   └── styles.css      # Стилі (112 KB)
├── js/
│   ├── core.js         # Весь JavaScript (1.9 MB)
│   ├── data.js         # Заглушка (буде розділено)
│   ├── effects.js      # Заглушка (буде розділено)
│   ├── ui.js           # Заглушка (буде розділено)
│   ├── fight.js        # Заглушка (буде розділено)
│   ├── training.js     # Заглушка (буде розділено)
│   ├── team.js         # Заглушка (буде розділено)
│   ├── career.js       # Заглушка (буде розділено)
│   ├── shop.js         # Заглушка (буде розділено)
│   ├── tournaments.js  # Заглушка (буде розділено)
│   ├── aging.js        # Заглушка (буде розділено)
│   └── init.js         # Ініціалізація (646 bytes)
└── README.md           # Документація
```

## ⚠️ Важливо

### Поточний Стан:
- ✅ Структура готова
- ✅ CSS виділено
- ✅ HTML готовий
- ⏳ JS поки що в core.js (не розділений)

### Наступні Кроки (Опціонально):
1. Розділити core.js на модулі
2. Оптимізувати кожен модуль
3. Додати lazy loading

### Для GitHub:
```bash
# Файл core.js (1.9 MB) може бути великий для GitHub
# Варіанти:

# 1. Використай Git LFS:
git lfs track "*.js"
git add .gitattributes
git add .
git commit -m "Add with LFS"

# 2. Або мініфікуй:
# (використай online minifier для core.js)

# 3. Або використай Releases:
# Завантаж core.js через GitHub Releases
```

## 🐛 Виправлення Проблем

### Гра не завантажується:
1. Перевір console (F12)
2. Переконайся що всі файли завантажились
3. Перевір шляхи до файлів

### Telegram не відкриває:
1. Переконайся що URL правильний
2. Перевір HTTPS (обов'язково!)
3. Протестуй спочатку в браузері

## 📞 Підтримка

Проблеми? Відкрий Issue на GitHub!

---

**Версія**: 0.91 Modular  
**Дата**: 29.12.2024
